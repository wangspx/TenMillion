<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.spwang.luck.tenmillion.Repository.AllCombinationRepository">

    <resultMap id="BaseResultMap" type="com.spwang.luck.tenmillion.entity.AllCombination">
        <result column="id" property="id"/>
        <result column="combination" property="combination"/>
        <result column="create_time" property="createTime"/>
        <result column="update_time" property="updateTime"/>
    </resultMap>

    <sql id="Base_Column_List">
            id,
            combination,
            create_time,
            update_time
    </sql>

    <select id="count" resultType="java.lang.Long">
        select count(*) from all_combination
    </select>

    <insert id="save" useGeneratedKeys="true" keyProperty="id">
        insert into all_combination (combination,create_time,update_time)
        value (#{combination}, #{createTime}, #{updateTime});
    </insert>

    <insert id="saveAll" useGeneratedKeys="true" keyProperty="id" parameterType="java.util.List">
        insert into all_combination (combination,create_time,update_time)
        values
        <foreach collection="list" item="item" separator=",">
            (#{item.combination}, #{item.createTime}, #{item.updateTime})
        </foreach>
         ;
    </insert>

<!--auto generated by MybatisCodeHelper on 2019-07-21-->
    <select id="selectAllById" resultMap="BaseResultMap">
        select
        <include refid="Base_Column_List"/>
        from all_combination
        where
        <if test="id != null">
            id=#{id}
        </if>
    </select>

<!--auto generated by MybatisCodeHelper on 2019-07-21-->
    <insert id="insertList" useGeneratedKeys="true" keyProperty="id">
        insert into all_combination(
        combination,
        create_time,
        update_time
        )VALUES
        <foreach collection="list" item="element" index="index" separator=",">
            (
            #{element.combination},
            #{element.createTime},
            #{element.updateTime}
            )
        </foreach>
    </insert>
</mapper>
